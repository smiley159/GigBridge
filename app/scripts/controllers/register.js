'use strict';

/**
 * @ngdoc function
 * @name angelHackApp.controller:RegisterCtrl
 * @description
 * # RegisterCtrl
 * Controller of the angelHackApp
 */
angular.module('angelHackApp')
  .controller('RegisterCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
